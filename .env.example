# Canonical origin — used for metadataBase, canonical URLs, sitemap and JSON-LD.
NEXT_PUBLIC_SITE_URL=https://www.yourdomain.com

# --- Lead storage (use Supabase and/or a webhook) ---
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: also forward leads to a CRM / Zapier / Make webhook
LEAD_WEBHOOK_URL=
LEAD_WEBHOOK_TOKEN=
